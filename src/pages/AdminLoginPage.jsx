import PageStub from '../components/PageStub'

// Placeholder only — real admin login UI is built in the admin dashboard
// phase, once the backend admin auth (already in place) has a UI to call it.
export default function AdminLoginPage() {
  return <PageStub title="Admin Login — coming soon" />
}
