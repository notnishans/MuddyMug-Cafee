# Base URL for the backend API. Leave empty in development — requests
# go out as relative paths and are caught by Vite's dev proxy (see
# vite.config.js), which forwards /api/* to http://localhost:4000.
# Set this to the deployed backend's URL for production builds.
VITE_API_URL=
